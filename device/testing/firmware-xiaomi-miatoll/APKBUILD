maintainer="Nikroks <nikroks@mainlining.org>"
pkgname=firmware-xiaomi-miatoll
pkgver=2
pkgrel=0
_commit="a7073ad14a91a103d14effbc87a94e632e29011a"
pkgdesc="Firmware for miatoll"
url="https://github.com/sm7125-mainline/firmware-xiaomi-miatoll"
arch="aarch64"
depends="linux-firmware-qcom linux-firmware-qca linux-firmware-ath10k"
license="proprietary"
subpackages="$pkgname-sensors:firmware_sensors"
options="!strip !check !archcheck !tracedeps textrels pmb:cross-native"
source="
	$pkgname.tar.gz::https://github.com/sm7125-mainline/firmware-xiaomi-miatoll/archive/$_commit/$pkgname-$_commit.tar.gz
	30-initramfs-firmware.files
	firmware.files
	sensor.files
"
builddir="$srcdir/$pkgname-$_commit"

package() {
	while IFS="" read -r _i || [ -n "$_i" ]; do
		local _u="${_i/usr\/lib/\/lib}"
		[ ! -d $(dirname $_i) ] && mkdir -p $(dirname "$pkgdir"/$_i)
		install -Dm644 .$_u "$pkgdir/$_i"
	done < "$srcdir/firmware.files"

	install -Dm644 "$srcdir/30-initramfs-firmware.files" -t \
		"$pkgdir"/usr/share/mkinitfs/files
}

firmware_sensors() {
	pkgdesc="Files to be included to add sensor support"
	depends="$pkgname=$pkgver-r$pkgrel"
	install_if="$pkgname-nonfree-firmware"
	mkdir "$subpkgdir"
	cd "$builddir"

	while IFS="" read -r _i || [ -n "$_i" ]; do
		local _u="${_i/usr\/lib/\/lib}"
		[ ! -d $(dirname $_i) ] && mkdir -p $(dirname "$subpkgdir"/$_i)
		install -Dm644 .$_u "$subpkgdir/$_i"
	done < "$srcdir/sensor.files"
}

sha512sums="
5535bbb86d41dabca255e76f0edd3032e58df1a8bee444dc26238119b224bdd8b3db97101439a81b9c24a16eeba106e8d0adf22904f0f810ee91008b69102a10  firmware-xiaomi-miatoll.tar.gz
f988769a926b54a13d51cd9cd81e65068a2e5504c128e41e710ea28494a36a61c743f5cc7aefc0a19c3df71de639cc92b8436386fbf8155ae5b734fac219aba1  30-initramfs-firmware.files
584e8bfaf842f86c4da08ad81909a90c794e665df1a5f1e1956ea34f141773042cc48b432329d8f1f875c5e39c9215cc6895a5dc4baa27586ceb11b986287342  firmware.files
3cd70e1565048f207a8b31e90cc4ae8505fced068db252018b892c9bd403c0466e14bf92cff2d1f3d4d6091eb4f034808b239d83d056dcbfe377b2f8b81d449c  sensor.files
"
